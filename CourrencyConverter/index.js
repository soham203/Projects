const baseLink = "https://cdn.jsdelivr.net/gh/fawazahmed0/currency-api@1/latest/currencies/eur/jpy.json";

const dropDowns = document.querySelectorAll(".dropdown select");

for (code in countryList) {
    console.log(code);
  } 